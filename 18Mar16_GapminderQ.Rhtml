<html>
#Takes a dataset and multiples population column with GDP per capita column

<head>
<title>Title</title>
</head>

<body>

<p>This is an R HTML document. When you click the <b>Continent GDP Analyzer</b> button a web page will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:</p>

<!--begin.rcode
calcGDPcont <- function(dat, year=NULL, continent=NULL) 
  {
  if(!is.null(year)) {
    dat <- dat[dat$year %in% year, ]
  }
  if (!is.null(continent)) {
    dat <- dat[dat$continent %in% continent, ]
  }
  gdp <- dat$pop * dat$gdpPercap
  new <- cbind(dat, gdp=gdp)
  return(new)
}
end.rcode-->
<!--begin.rcode

end.rcode-->

<p>You can also embed plots, for example:</p>

<!--begin.rcode fig.width=7, fig.height=6
ggplot(data = gapminder, aes(x = continent, y = gdpPercap, by=lifeExp, color=continent)) +
  geom_point()
end.rcode-->

</body>
</html>
